# Archived environment notes; not rerun after curation

The original notebook used Python, PyTorch, PyTorch Geometric, NumPy, scikit-learn, matplotlib, `transformers`, `gdown`, `tqdm`, MultiBench, CLIP, and a CUDA-capable GPU. Some preprocessing also used Gurobi.

External AV-MNIST and graph–text data, model downloads, solver-generated MPS files, graph artifacts, checkpoints, local paths, and environment caches are intentionally excluded. The notebook is preserved for inspection rather than as a turnkey runtime.
