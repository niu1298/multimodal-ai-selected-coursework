# Archived environment notes; not rerun after extraction

The source notebook indicates the following runtime assumptions:

- Python with PyTorch, `transformers`, `peft`, `trl`, `datasets`, and Pillow.
- A CUDA-capable GPU that supports bf16 operations.
- Access to `Qwen/Qwen3-VL-2B-Instruct` and any associated model downloads.
- External JSONL records and corresponding images in the layout expected by the original coursework.

The original work uses LoRA and `trl.GRPOTrainer`. No dependency versions, data download commands, credentials, checkpoints, or runtime setup are reproduced here, and no environment was recreated or rerun during extraction.
