# Archived environment notes; not rerun after extraction

The original source indicates these external assumptions:

- Python and `smolagents` for the optional tool interface.
- `matplotlib` for the retained plotting helpers.
- A separately configured data root containing JSONL records with image, question, and answer fields.
- A vision-language-model runtime and any related model/API access used in the original coursework.

No credentials, data, solver licenses, model weights, traces, or runnable deployment configuration are included. The original package had no portable entry point or package metadata, and no environment was recreated or rerun after extraction.
