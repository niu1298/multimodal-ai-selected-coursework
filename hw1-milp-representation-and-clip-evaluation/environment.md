# Archived environment notes; not rerun after curation

The original notebook used Python with NumPy, SciPy, pandas, NetworkX, scikit-learn, matplotlib, seaborn, Pillow, PyTorch, `datasets`, and `transformers`.

The Hateful Memes bonus experiment also depended on separately obtained dataset records/images and pretrained CLIP assets. Data downloads, image caches, local output directories, and model files are intentionally excluded.
