# Archived environment notes; not rerun after curation

The original work used Python, PyTorch, `transformers`, `peft`, PIL, JSONL data, a Qwen2.5-VL-3B model, and a CUDA-capable environment. The notebook also uses rendering and plotting utilities for MILP heatmaps.

External image/JSONL data, adapter checkpoints, caches, Drive storage, local output paths, and runtime environment files are intentionally excluded.
